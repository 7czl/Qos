[package]
name = "qos-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.13", optional = true }

[dev-dependencies]
proptest = "1"
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
